{% for _billStatus in billStatus %}
    <tr>
        <td>{{ _billStatus.id }}</td>
        <td>{{ _billStatus.name }}</td>
        <td class="text-center">
            <div class="btn-group">
                <a href="{{ path('flexphp.invoice.bill-status.read', {id: _billStatus.id}) }}" class="btn btn-sm btn-outline-light" title="{% trans from 'messages' %}action.read{% endtrans %}">
                    <i class="fa fa-eye text-dark" aria-hidden="true"></i>
                </a>

                <a href="{{ path('flex.invoice.bill-status.edit', {id: _billStatus.id}) }}" class="btn btn-sm btn-outline-light" title="{% trans from 'messages' %}action.edit{% endtrans %}">
                    <i class="fa fa-edit text-info" aria-hidden="true"></i>
                </a>
            </div>
        </td>
    </tr>
{% endfor %}
